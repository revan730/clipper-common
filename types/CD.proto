syntax = "proto3";
package types;

message Deployment {
    int64 ID = 1;
	string Branch = 2;
	int64 RepoID = 3;
	int64 ArtifactID = 4;
	string K8SName = 5;
	string Manifest = 6;
}

message Empty {}

service CDAPI {
    rpc CreateDeployment(Deployment) returns (Empty) {}
}