syntax = "proto3";
package types;

message Deployment {
    int64 ID = 1;
	string Branch = 2;
	int64 ArtifactID = 3;
	string K8SName = 4;
	string Manifest = 5;
}

message Empty {}

service CDAPI {
    rpc CreateDeployment(Deployment) returns (Empty) {}
}